<!--index.wxml-->

<view class="body">

    <drawimage bind:toTempFile="toTempFile" id="drawimage1" height="1000" background="{{background}}" layers="{{layers}}" bind:download="download" />

    <button bindtap="download">下载</button>

    <drawimage id="drawimage1" height="1000" background="{{background}}" layers="{{layers}}" bind:download="download" />

</view>

